server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/acelerati?currentSchema=stock
    username: postgres
    password: 11Orange3526

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
